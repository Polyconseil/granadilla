[bdist_wheel]
universal = 1
python-tag = py3

[zest.releaser]
; semver-style versions
version-levels = 3

; tags: vX.Y.Z; the double-percent is a guard against ConfigParser.
#tag-format = v%%(version)s

; Version flag location (we use __version__)
python-file-with-version = granadilla/version.py
